export default function Duas() {
  return (
    <main className="container">
      <h1>Duas</h1>
      <p className="lead">Supplications by occasion, with Arabic, transliteration, translation and references.</p>
      <div className="soon">Awaiting import — content lives in <code>content/duas</code>.</div>
    </main>
  );
}
